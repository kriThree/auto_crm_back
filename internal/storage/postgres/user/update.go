package storage_user

import (
	"context"
	storage_models "server_crm/internal/storage/models"
)

func (s UserStorage) Update(ctx context.Context, dto storage_models.UpdateUserDto) error {
	return nil
}
